package com.animal_villa.dto
/**
 * A class Player is noun class Data Transfer Object.
 * @property status the status for every Player : Int
 * @property money the money for every Player : Int
 * @property energy the energy for every Player : Int
 * @property currentLocation the currentLocation for every Player : Int
 */
data class Player(var status : Int, var money : Int, var energy : Int, var currentLocation : Int) {
    /**
     * Contains all the methods for data class player
     */

}